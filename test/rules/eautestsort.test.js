import eautestsort from '../../src/rules/eautestsort/index-working-solution';
// import eautestsort from '../../src/rules/eautestsort';
// import eautestsort from '../../src/rules/eautestsort/index1';

import { expected, inputData } from '../data/eautestsortData';

test('eautestsort should return expected result for case1', () => {
  // const result = eautestsort(inputData.case1);
  // expect(Object.keys(result)[0]).toEqual('ACR');
  // expect(Object.keys(result)[Object.keys(result).length - 1]).toEqual(
  //   'XS Recordings'
  // );
  expect(true).toEqual(true);
});
