/**
 * @param {number[]} stones
 * @return {number}
 */
var stoneGameVII = function(stones) {
  /**
   * Alice:  aliceTotal+= Max(arr[0], arr[arr.length-1]);
   * Bob: bobTotal+= Max(dp(arr));
   * return aliceTotal -bobTotal;
   */
  return NaN;
};

export default stoneGameVII;
