/**
 * @param {string} order
 * @param {string} s
 * @return {string}
 */
/**
 * my thought: O(n)
 * build a set from s
 * for each char in order, if exists in s-Set,
 *      append to result string && remove the char from s-Set
 * for each char in s-Set, append it to result string
 */
var customSortString = function(order, s) {
  return null;
};

export default customSortString;
