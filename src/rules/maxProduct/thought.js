/**
 * @param {number[]} nums
 * @return {number}
 */
var maxProduct = function(nums) {
  // greedy

  // just two base cases, either take current, or not take current
  // dp[i] = Math.max(dp[i-1]+dp[i], dp[i])
  // finally return dp[nums.length]
  return NaN;
};

export default maxProduct;
