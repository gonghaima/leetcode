/**
 * @param {number[]} temperatures
 * @return {number[]}
 */
var dailyTemperatures = function(temperatures) {
  // thoughts
  // declare result array
  // declare tracking array
  // loop the temperatures from the end to the start position
  // the last temperature is zero, add the zero the result, add temperate to the tracking array
  // for each temperature, add find the index of first greater value, compare current index, add the diff to the result
  // return result

  // question? how to make tracking array more effecient, to find out greater value with min index
};

export default dailyTemperatures;
